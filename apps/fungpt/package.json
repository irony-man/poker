{
  "name": "@poker/fungpt",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sh -c 'exec ./.venv/bin/python -m uvicorn server:app --host 127.0.0.1 --port 8000 --reload'",
    "start": "sh -c 'exec ./.venv/bin/python -m uvicorn server:app --host 0.0.0.0 --port 8000'",
    "test": "sh -c 'exec ./.venv/bin/python -m unittest discover -s . -p \"test_*.py\"'"
  }
}
