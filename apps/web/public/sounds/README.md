# Table SFX

Default table action / street samples used when site config has no override URLs.

These files were synthesized for POKR (short procedural tones). Replace any file in place, or set alternate paths/URLs in Admin → Sounds.
